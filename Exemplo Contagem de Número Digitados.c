#include <stdio.h>
int main(){
	int i=0, n=0;
	printf("Digite o valor de n = ")/
	scanf("%d", &n);
	for(i=1; i<=n ; i++){
		if(i <= n){
			if(i%2 == 0 ){
				printf("%d - ", i);
			}
		}
	 else {
		printf("%d", n);
	}
}
		return 0;
	}
