#include <stdio.h>

int main()
{
   float real, dolar;
    printf("Digite o valor em R$");
    scanf("%f", &real);
    dolar = 5*real;
    printf("A conversão de real para dólares é US$:%.2f", dolar);
    return 0 ;
}
